# 此配置未启用 - finalstock 项目通过以下方式访问：
#
# 开发模式（推荐）:
#   cd frontend && npm run dev   # 访问 http://your-ip:5173
#   后端 API 在 3000 端口
#
# 生产模式:
#   使用任意静态服务器托管 frontend/dist 目录
#
# Nginx 配置示例（如果需要）:
# server {
#     listen 80;
#     server_name your-domain.com;
#
#     root /path/to/finalstock/frontend/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }